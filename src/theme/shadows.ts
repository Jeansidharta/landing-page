export type DefaultThemeShadows = {
	button: string;
	navbar: string;
	card: string;
	benefit: string;
};

export const DefaultThemeShadows = {
	button: '-4px 4px 4px rgba(0, 0, 0, 0.4)',
	navbar: '-12px 12px 12px rgba(0, 0, 0, 0.4)',
	card: '-4px 4px 12px rgba(0, 0, 0, 0.4)',
	benefit: '-1px 1px 4px rgba(0, 0, 0, 0.4)',
} as DefaultThemeShadows;
